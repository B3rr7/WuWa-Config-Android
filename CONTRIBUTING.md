# Contributing
**Quick start:** JDK 17, Android SDK API 36.
**Commands:** `./gradlew ktlintCheck testDebugUnitTest assembleDebug`
**Code style:** Run `./gradlew ktlintFormat` to auto-format.
**Branches:** Use `fix/<slug>` or `feat/<slug>`. Open PRs targeting `main`.